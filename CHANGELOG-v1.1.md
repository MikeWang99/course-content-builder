# v1.1.0

- Split scope location from official requirement extraction.
- Added stable requirement/constraint IDs in `requirements.json`.
- Added machine-checkable `coverage.json` as a pre-generation hard gate.
- Added `validate_requirements.py` and `validate_coverage.py`.
- Upgraded `validate_run.py` to enforce all three gates.
- Refactored the default teaching prompt to preserve the original pedagogical strengths without mechanically repeating every pattern.
- Added explicit source authority, extension, provenance, and Obsidian math rules.
