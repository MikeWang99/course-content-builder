interface:
  display_name: "Course Builder"
  short_description: "从 syllabus 定位范围，再生成可追溯的单元教学内容"
  default_prompt: "Use $course-builder. Never start course prose before locating and validating the requested syllabus scope. Follow REQUEST → INVENTORY → LOCATE → EXTRACT → ENRICH → COVERAGE PLAN → GENERATE → VALIDATE → OUT, and generate only the bounded unit/chapter the user asked for."
